enum UserRole {
    TENANT
    LANDLORD
    ADMIN
}

enum UserStatus {
    ACTIVE
    BANNED
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    UNAVAILABLE
}

enum RentalStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentProvider {
    STRIPE
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}